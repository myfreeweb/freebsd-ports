GNOME LaTeX (previously called LaTeXila) is a LaTeX editor for the GNOME desktop.
The idea of GNOME LaTeX is to always deal directly with the LaTeX code, while
simplifying as much as possible the writing of this LaTeX code.

WWW: https://wiki.gnome.org/Apps/GNOME-LaTeX
