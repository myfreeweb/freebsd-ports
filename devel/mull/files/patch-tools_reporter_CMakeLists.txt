--- tools/reporter/CMakeLists.txt.orig	2018-10-06 15:11:37 UTC
+++ tools/reporter/CMakeLists.txt
@@ -1,3 +1,6 @@
+find_package(PkgConfig)
+pkg_check_modules(SQLITE3 sqlite3 IMPORTED_TARGET)
+
 add_executable(mull-reporter
   WeakTestsReporter.h
   WeakTestsReporter.cpp
@@ -10,7 +13,7 @@ set_target_properties(mull-reporter PROPERTIES
 )
 target_link_libraries(mull-reporter
   LLVMSupport
-  sqlite3
+  PkgConfig::SQLITE3
 )
 target_include_directories(mull-reporter PUBLIC
   ${MULL_INCLUDE_DIRS}
